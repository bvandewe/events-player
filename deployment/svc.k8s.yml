apiVersion: v1
kind: Service
metadata:
  name: cloudevent-viewer
  namespace: mozart
spec:
  ports:
    - port: 80
      targetPort: 8888
      protocol: TCP
      name: web
  selector:
    app: cloudevent-viewer
