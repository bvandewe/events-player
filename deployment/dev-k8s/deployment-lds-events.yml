apiVersion: apps/v1
kind: Deployment
metadata:
  name: lds-events
  namespace: mozart
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lds-events
      version: v1
  template:
    metadata:
      labels:
        app: lds-events
        version: v1
    spec:
      containers:
      - name: lds-events
        image: ccie-gitlab.ccie.cisco.com:4567/mozart/infrastructure/eventing/cloudevent-player:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        env:
        - name: api_default_generator_gateways
          value: '{"urls": ["http://localhost/events/pub", "http://k.ccie.cisco.com:32051/events/pub"]}'
        - name: api_browser_queue_size
          value: "1000"
        - name: api_tag
          value: "0.1.14"
        - name: api_default_generator_event
          value: '{"event_source": "https://dummy-lds.certs.cisco.com", "event_type":"com.cisco.certs.lds.test.requested.v1"}'
